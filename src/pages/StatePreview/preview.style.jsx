import styled from 'styled-components'

const Card = styled.div``

const Box = styled.div``

const Total = styled.div`
  display: flex;
  align-items: center;

  gap: 1rem;
`

const Image = styled.img``

export { Box, Total, Card, Image }
